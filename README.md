# JUnit to Cuppa Conversion Workshop

This project contains some JUnit tests and the skeleton of some Cuppa tests.
The goal is to convert the JUnit tests into Cuppa tests.

To run the tests:

```
$ mvn test -q
```
